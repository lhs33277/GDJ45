Map 인터페이스

1. 하나의 데이터를 '키(key)-값(value)'으로 구성하는 자료구조이다.
2. '키(key)'를 '변수', '값(value)'을 '변수에 저장된 값'으로 생각하면 된다.
3. 특징
	1) 키(key)는 중복이 불가능하다. 값을 얻기 위해 필요한 데이터이다.
	2) 값(value)은 중복이 가능하다. 실제로 저장하고자 하는 데이터이다.
4. HashMap 클래스
	1) 해시 기반의 Map 처리 클래스이다.